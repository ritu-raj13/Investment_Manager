# Production Environment Variables
# Set these as environment variables in your deployment platform
# (Railway, Render, Heroku, etc.)

# Flask Configuration
FLASK_ENV=production
SECRET_KEY=generate-a-strong-random-secret-key-here

# Admin Credentials (single user authentication)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your-very-secure-password

# Database (PostgreSQL for production)
# Format: postgresql://username:password@host:port/database
# This is usually provided automatically by your cloud platform
DATABASE_URL=postgresql://user:password@host:5432/dbname

# Frontend URL (for CORS)
FRONTEND_URL=https://your-frontend-domain.com

# Optional: Redis for rate limiting (if not using memory://)
# REDIS_URL=redis://...

# Security Notes:
# - Never commit actual credentials to version control
# - Use strong, randomly generated passwords
# - Rotate SECRET_KEY periodically
# - Use your platform's secrets management when possible
